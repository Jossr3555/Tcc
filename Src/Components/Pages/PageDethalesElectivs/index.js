import * as React from 'react';
import { View, SafeAreaView, Text, TextInput, TouchableOpacity } from 'react-native';

export default function DethalesScream ({ navigation, route }){
    return(
        <View>

        </View>
    );
}